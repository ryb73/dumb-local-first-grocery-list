Quick instructions to get it running:

1. Install workspace dependencies with pnpm – `pnpm i`
2. In `back-end/`, run `pnpm run dev`
3. In `front-end/`, run `pnpm run dev`
4. Open `https://localhost:3000`
